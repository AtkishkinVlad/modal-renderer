import { beforeEach, afterEach } from 'vitest';

// Очищаем Event Bus перед каждым тестом
beforeEach(() => {
  // Здесь можно добавить логику очистки, если нужно
});

afterEach(() => {
  // Очистка после тестов
});
