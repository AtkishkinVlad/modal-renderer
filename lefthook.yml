pre-push:
  commands:
    lint:
      tags: lint files
      run: npm run lint

pre-commit:
  parallel: true
  commands:
    format:
      tags: format files
      run: npm run format {staged_files}
    commit-lint:
      tags: check conventional commits
      run: npx commitlint --from=HEAD
